import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  root: {
    flex: 1,
    // position: "absolute",
    bottom: 0,
    height: "100%",
  },
  flexOne: {
    // position: "absolute",
    bottom: 0,
    flex: 1,
  },
});

export default styles;
